}
for(i in 2:30){
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]), rownames(sig_miRNA)[i])
}
correlations
correlations <- cbind.data.frame(correlations, rownames(sig_miRNA))
correlations <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]))
}
correlations <- cbind.data.frame(correlations, rownames(sig_miRNA))
for(miRNA in rownames(lihc_dataM)){
miR_row <- lihc_dataM[miRNA,]
m <- c(m, mean(miR_row))
sd <- c(sd, sd(miR_row))
miRNA_pval <- t.test(miR_row[1:49], miR_row[50:98])$p.val
if(!is.na(miRNA_pval) & miRNA_pval < 0.00001){
pval <- c(pval, miRNA_pval)
}
}
results <- as.data.frame(cbind(m, sd, pval))
rownames(results) <- rownames(lihc_dataM)
View(results)
pval <- c(pval, miRNA_pval)
if(!is.na(miRNA_pval) & miRNA_pval < 0.00001){
pval <- c(pval, miRNA_pval)
}
for(miRNA in rownames(lihc_dataM)){
miR_row <- lihc_dataM[miRNA,]
m <- c(m, mean(miR_row))
sd <- c(sd, sd(miR_row))
miRNA_pval <- t.test(miR_row[1:49], miR_row[50:98])$p.val
if(!is.na(miRNA_pval) & miRNA_pval < 0.00001){
pval <- c(pval, miRNA_pval)
}
}
miRNA_pval <- t.test(miR_row[1:49], miR_row[50:98])$p.val
if(!is.na(miRNA_pval) & miRNA_pval < 0.00001){
pval <- c(pval, miRNA_pval)
}
if(!is.na(miRNA_pval) & miRNA_pval < 0.00001){
pval <- c(pval, miRNA_pval)
}
for(miRNA in rownames(lihc_dataM)){
miR_row <- lihc_dataM[miRNA,]
m <- c(m, mean(miR_row))
sd <- c(sd, sd(miR_row))
miRNA_pval <- t.test(miR_row[1:49], miR_row[50:98])$p.val
if(!is.na(miRNA_pval) & miRNA_pval < 0.00001){
pval <- c(pval, miRNA_pval)
}
}
for(miRNA in rownames(lihc_dataM)){
miR_row <- lihc_dataM[miRNA,]
m <- c(m, mean(miR_row))
sd <- c(sd, sd(miR_row))
miRNA_pval <- t.test(miR_row[1:49], miR_row[50:98])$p.val
if(!is.na(miRNA_pval)){
pval <- c(pval, miRNA_pval)
}
}
results <- as.data.frame(cbind(m, sd, pval))
rownames(results) <- rownames(lihc_dataM)
correlations <- cbind.data.frame(correlations, rownames(sig_miRNA)[,1])
correlations <- cbind.data.frame(correlations, rownames(sig_miRNA)[1])
View(correlations)
correlations <- cbind.data.frame(correlations, rownames(sig_miRNA))
correlations <- cbind.data.frame(correlations, rownames(sig_miRNA)[2:30])
View(correlations)
correlations <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]))
}
correlations <- cbind.data.frame(correlations, rownames(sig_miRNA)[2:30])
View(correlations)
most_cor <- vector()
for(i in correlations){
print(i)
}
rownames(correlations) <- sig_miRNA[2:30]
correlations <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]))
}
rownames(correlations) <- sig_miRNA[2:30]
sig_miRNA[2:30]
correlations <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]))
}
rownames(correlations) <- rownames(sig_miRNA)[2:30]
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]))
correlations <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]))
}
correlations <- as.data.frame(correlations)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
View(correlations)
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]), rownames(sig_miRNA[i,]))
correlations <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]), rownames(sig_miRNA[i,]))
}
correlations <- as.data.frame(correlations)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
View(correlations)
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]), rownames(sig_miRNA[i,]))
View(correlations)
rownames(sig_miRNA[i,])
rownames(sig_miRNA[i])
rownames(sig_miRNA[i])
rownames(sig_miRNA)[i]
correlations <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]), rownames(sig_miRNA)[i])
}
correlations <- as.data.frame(correlations)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
View(correlations)
View(correlations)
rownames(sig_miRNA)[2:30]
correlations <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]))
}
correlations <- as.data.frame(correlations)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
correlations
most_cor <- vector()
for(i in correlations){
print(i)
}
max(correlations)
which(max(correlations))
?which
which(correlations, max(correlations))
which.max(correlations)
most_cor <- vector()
which.max(correlations)
which.max(correlations[,1])
correlations <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]))
}
correlations <- as.data.frame(correlations)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
correlations
View(correlations)
rownames(correlations)[which.max(correlations[,1])]
gt(results)
knitr::opts_chunk$set(echo = TRUE)
knitr::opts_chunk$set(message = FALSE)
knitr::opts_chunk$set(warning = FALSE)
paged_table(results)
rmarkdown::paged_table(results)
paged_table(sig_miRNA)
rmarkdown::paged_table(sig_miRNA)
rmarkdown::paged_table(rownames(sig_miRNA))
rownames(sig_miRNA)
paste(sig_miRNA, sep=", ")
paste(rownames(sig_miRNA), sep=", ")
rownames(sig_miRNA)
paste(shQuote(rownames(sig_miRNA)), sep=", ")
paste(miRNAs, sep=", ")
miRNAs <- rownames(sig_miRNA)
paste(miRNAs, sep=", ")
paste(rownames(sig_miRNA), sep=", ")
rmarkdown::paged_table(correlations)
rownames(correlations)[which.max(correlations[,1]) + 1]
max <- rownames(correlations)[which.max(correlations[,1])]
second <- rownames(correlations)[which.max(correlations[,1]) + 1]
max
correlations <- correlations[order(correlations$correlations)]
correlations <- correlations[order(correlations$correlations),]
miRNA_correlations <- rownames(correlations)
miRNA_correlations[1]
correlations <- correlations[order(correlations$correlations),]
miRNA_correlations <- rownames(correlations)
[1]
miRNA_correlations[1]
correlations <- correlations[order(correlations$correlations),]
correlations <- correlations[order(correlations),]
correlations <- correlations[order(correlations[1,]),]
correlations <- correlations[order(correlations[,1]),]
correlations <- correlations[order(correlations[1]),]
correlations[1]
correlations[1,]
correlations <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]))
}
rownames(correlations) <- rownames(sig_miRNA)[2:30]
correlations <- as.data.frame(correlations)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
rmarkdown::paged_table(correlations)
correlations <- correlations[order(correlations$correlations),]
correlations <- vector()
most_sig <- sig_miRNA[1,]
correlations <- as.data.frame(correlations)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
rmarkdown::paged_table(correlations)
correlations <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]))
}
correlations <- as.data.frame(correlations)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
rmarkdown::paged_table(correlations)
sorted_correlations <- correlations[order(correlations$correlations),]
miRNA_correlations <- rownames(sorted_correlations)
miRNA_correlations[2]
miRNA_correlations[1]
miRNA_correlations
sorted_correlations <- correlations[order(correlations$correlations),]
sorted_correlations
rownames(sorted_correlations)
sorted_correlations <- correlations[order(correlations$correlations),]
miRNA_correlations <- rownames(sorted_correlations, descending=TRUE)
miRNA_correlations
sorted_correlations <- correlations[order(correlations$correlations),]
order(correlations$correlations)
correlations[order(correlations$correlations)]
correlations[order(correlations$correlations),]
view(correlations[order(correlations$correlations),])
options(scipen=10)
correlations[order(correlations$correlations),]
correlations[order(correlations$correlations),]
corre <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]))
}
correlations <- as.data.frame(corre)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
corre <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
correlations <- c(correlations, cor(most_sig, sig_miRNA[i,]))
}
corre <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
corre <- c(corre, cor(most_sig, sig_miRNA[i,]))
}
correlations <- as.data.frame(corre)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
rmarkdown::paged_table(correlations)
correlations[order(correlations$corre),]
sorted_correlations <- correlations[order(correlations$correlations),]
sorted_correlations <- correlations[order(correlations$corre),]
sorted_correlations <- as.data.frame(correlations[order(correlations$corre),])
View(sorted_correlations)
corre <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
corre <- c(corre, cor(most_sig, sig_miRNA[i,]))
}
correlations <- as.data.frame(corre)
View(correlations)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
View(correlations)
rmarkdown::paged_table(correlations)
correlations[order(correlations$corre),]
rownames(correlations[order(correlations$corre),])
correlations <- rownames(correlations[order(correlations$corre),])
corre <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
corre <- c(corre, cor(most_sig, sig_miRNA[i,]))
}
rownames(correlations) <- rownames(sig_miRNA)[2:30]
rmarkdown::paged_table(correlations)
corre <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
corre <- c(corre, cor(most_sig, sig_miRNA[i,]))
}
correlations <- as.data.frame(corre)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
rmarkdown::paged_table(correlations)
correlations <- correlations[order(correlations$corre),]
correlations <- correlations[order(correlations$corre),]
sorted_results <- results[order(results$pval),]
View(sorted_results)
sorted_correlations <- correlations[order(correlations$corre),]
corre <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
corre <- c(corre, cor(most_sig, sig_miRNA[i,]))
}
correlations <- as.data.frame(corre)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
rmarkdown::paged_table(correlations)
sorted_correlations <- correlations[order(correlations$corre),]
miRNA_correlations <- correlations[rownames(sorted_correlations),]
correlations <- cbind.data.frame(corre)
corre <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
corre <- c(corre, cor(most_sig, sig_miRNA[i,]))
}
correlations <- data.frame()
correlations <- cbind.data.frame(corre)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
rmarkdown::paged_table(correlations)
View(correlations)
sorted_correlations <- correlations[order(correlations$corre),]
miRNA_correlations <- correlations[rownames(sorted_correlations),]
miRNA_correlations
correlations <- data.frame()
corre <- vector()
rows <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
corre <- c(corre, cor(most_sig, sig_miRNA[i,]))
rows <- c(rows, i)
}
correlations <- cbind.data.frame(corre, rows)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
rmarkdown::paged_table(correlations)
correlations <- data.frame()
corre <- vector()
rows <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
corre <- c(corre, cor(most_sig, sig_miRNA[i,]))
rows <- c(rows, i - 1)
}
correlations <- cbind.data.frame(corre, rows)
rownames(correlations) <- rownames(sig_miRNA)[2:30]
rmarkdown::paged_table(correlations)
gene <- c(gene, rownames(sig_miRNA[i,]))
correlations <- data.frame()
corre <- vector()
gene <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
corre <- c(corre, cor(most_sig, sig_miRNA[i,]))
gene <- c(gene, rownames(sig_miRNA[i,]))
}
correlations <- data.frame()
corre <- vector()
gene <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
corre <- c(corre, cor(most_sig, sig_miRNA[i,]))
gene <- c(gene, rownames(sig_miRNA[i,]))
}
correlations <- cbind.data.frame(corre, gene)
gene <- c(gene, rownames(sig_miRNA[i,]))
rownames(sig_miRNA[i,])
sig_miRNA
View(sig_miRNA)
rownames(sig_miRNA)
rownames(sig_miRNA[1])
rownames(sig_miRNA[1,])
sig_miRNA[1,]
rownames(sig_miRNA[1,])
gene <- c(gene, rownames(sig_miRNA)[i])
for(i in 2:30){
corre <- c(corre, cor(most_sig, sig_miRNA[i,]))
rownames(sig_miRNA[i,])
gene <- c(gene, rownames(sig_miRNA)[i])
}
correlations <- cbind.data.frame(corre, gene)
correlations <- data.frame()
corre <- vector()
gene <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
corre <- c(corre, cor(most_sig, sig_miRNA[i,]))
rownames(sig_miRNA[i,])
gene <- c(gene, rownames(sig_miRNA)[i])
}
correlations <- cbind.data.frame(corre, gene)
rownames(correlations) <- 1:nrow(correlations)
rmarkdown::paged_table(correlations)
sorted_correlations <- correlations[order(correlations$corre),]
View(sorted_correlations)
The most correlated miRNA with the most significant miRNA is **`r correlations[which.max(correlations[,1]), gene]`**.
The most correlated miRNA with the most significant miRNA is **`r correlations[which.max(correlations[,1]), gene]`**.
The most correlated miRNA with the most significant miRNA is **`r correlations[which.max(correlations[,1]), 2]`**.
correlations[which.max(correlations[,1]), 2]
correlations[which.main(correlations[,1]), 2]
correlations[which.max(correlations[,1]), 2]
correlations[which.min(correlations[,1]), 2]
correlations[which.max(correlations[,1]), 1]
correlations[which.min(correlations[,1]), 1]
sorted_correlations <- correlations[order(correlations$corre),]
miRNA_correlations <- correlations[rownames(sorted_correlations),]
View(miRNA_correlations)
View(sorted_correlations)
miRNA_correlations <- sorted_correlations[,2]
miRNA_correlations
sorted_correlations <- correlations[order(correlations$corre, descending=T),]
?order
sorted_correlations <- correlations[order(correlations$corre, decreasing=T),]
results
rownames(results)
View(results)
name <- vector()
m <- vector()
sd <- vector()
pval <- vector()
for(miRNA in rownames(lihc_dataM)){
miR_row <- lihc_dataM[miRNA,]
m <- c(m, mean(miR_row))
sd <- c(sd, sd(miR_row))
pval <- c(pval, t.test(miR_row[1:49], miR_row[50:98])$p.val)
}
results <- as.data.frame(cbind(m, sd, pval))
rownames(results) <- rownames(lihc_dataM)
rmarkdown::paged_table(results)
View(results)
rownames(results)
rownames(results)[miRNA_correlations[1]]
rownames(results)[miRNA_correlations[1],]
rownames(results)[miRNA_correlations[1]]
miRNA_correlations[1]
rownames(results)
rownames(results)[miRNA_correlations[1]]
match(rownames(results), miRNA_correlations[1])
rownames(results)
results_rownames <- rownames(results)
match(results_rownames, miRNA_correlations[1])
which(miRNA_correlations[1] %in% results_rownames)
match(results_rownames, miRNA_correlations[1])
match(c(4, 8), 1)
match(c(4, 8), 3=4')
match(c(4, 8), 4)
match(miRNA_correlations[1, results_rownames])
match(miRNA_correlations[1], results_rownames)
match(miRNA_correlations[1], rownames(results))
rownames(results)[results_index_max]
results_index_max <- match(miRNA_correlations[1], rownames(results))
rownames(results)[results_index_max]
results[results_index_max]
results_index_max <- match(miRNA_correlations[1], rownames(results))
results[results_index_max]
results[results_index_max,]
results[results_index_max,pval]
results[results_index_max, "pval"]
results[match(miRNA_correlations[1], rownames(results)), "pval"]
results[match(miRNA_correlations[2], rownames(results)), "pval"]
results[match(miRNA_correlations[3], rownames(results)), "pval"]
ratio <- c(ratio, mean(miR_row[1:49])/mean(miR_row[50:98]))
name <- vector()
m <- vector()
sd <- vector()
pval <- vector()
ratio <- vector()
for(miRNA in rownames(lihc_dataM)){
miR_row <- lihc_dataM[miRNA,]
m <- c(m, mean(miR_row))
sd <- c(sd, sd(miR_row))
pval <- c(pval, t.test(miR_row[1:49], miR_row[50:98])$p.val)
ratio <- c(ratio, mean(miR_row[1:49])/mean(miR_row[50:98]))
}
results <- as.data.frame(cbind(m, sd, pval, ratio))
rownames(results) <- rownames(lihc_dataM)
rmarkdown::paged_table(results)
results[match(miRNA_correlations[3], rownames(results)), "ratio"]
results[match(miRNA_correlations[2], rownames(results)), "ratio"]
results[match(miRNA_correlations[1], rownames(results)), "ratio"]
results[match(miRNA_correlations[29], rownames(results)), "ratio"]
results[match(miRNA_correlations[2], rownames(results)), "ratio"]
results[match(miRNA_correlations[1], rownames(results)), "ratio"]
The most correlated miRNA with the most significant miRNA is **`r correlations[which.max(correlations[,1]), 2]`** with a correlation of `r correlations[which.max(correlations[,1]), 1]`.
correlations[which.max(correlations[,1]), 2]
correlations <- cbind.data.frame(corre, miR)
correlations <- data.frame()
corre <- vector()
miR <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
corre <- c(corre, cor(most_sig, sig_miRNA[i,]))
rownames(sig_miRNA[i,])
miR <- c(miR, rownames(sig_miRNA)[i])
}
rownames(correlations) <- 1:nrow(correlations)
correlations <- data.frame()
corre <- vector()
miR <- vector()
most_sig <- sig_miRNA[1,]
for(i in 2:30){
corre <- c(corre, cor(most_sig, sig_miRNA[i,]))
rownames(sig_miRNA[i,])
miR <- c(miR, rownames(sig_miRNA)[i])
}
correlations <- cbind.data.frame(corre, miR)
View(correlations)
lihc_data_m_t <- t(lihc_dataM)
lihc_data_m_t <- t(lihc_dataM)
library(randomForest)
lihc_miR_data <- read.table("LIHC.txt", header = T, row.names = 1, stringsAsFactors = F)
lihc_miR_data <- as.data.frame(lihc_miR_data)
conditions <- rownames(lihc_miR_data)
rownames(lihc_miR_data) <- c(rep("Tumor", 49), rep("Control", 49))
conditions <- rownames(lihc_miR_data)
View(lihc_miR_data)
